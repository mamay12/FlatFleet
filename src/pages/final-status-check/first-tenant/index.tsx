import {Typography} from "antd"

const {Text} = Typography

function FirstTenant() {
    return <Text type='secondary' style={{textAlign: "center"}}>The system will check if you are the only tenant of</Text>
}

export default FirstTenant
